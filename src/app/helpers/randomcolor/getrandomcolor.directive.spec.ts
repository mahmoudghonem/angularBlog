import { GetrandomcolorDirective } from './getrandomcolor.directive';

describe('GetrandomcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new GetrandomcolorDirective();
    expect(directive).toBeTruthy();
  });
});
